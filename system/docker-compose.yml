services:
  indian-stocks-dbaas:
    build:
      context: ..
      dockerfile: system/Dockerfile
    container_name: indian-stocks-dbaas
    environment:
      API_PROFILE: ${API_PROFILE:-full}
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data
    restart: unless-stopped
